# This workflow uses actions that are not certified by GitHub.
# They are provided by a third-party and are governed by
# separate terms of service, privacy policy, and support
# documentation.

# This workflow will install Deno and run tests across stable and nightly builds on Windows, Ubuntu and macOS.
# For more information see: https://github.com/denolib/setup-deno

name: Deno

on:
  push:
    branches: [jentest]
  pull_request:
    branches: [jentest]

jobs:
  test:
    runs-on: ${{ matrix.os }} # runs a test on Ubuntu, Windows and macOS

    strategy:
      matrix:
#         deno: ["v1.x", "nightly"]
        deno: ["v1.x"]
        os: [ubuntu-latest]

    steps:
      - name: Setup repo
        uses: actions/checkout@v2

#       - name: Setup Deno
#         uses: denolib/setup-deno@c7d7968ad4a59c159a777f79adddad6872ee8d96
#         with:
#           deno-version: ${{ matrix.deno }} # tests across multiple Deno versions

#       - name: Cache Dependencies
#         run: deno cache deps.ts

#       - name: Run Tests
#         run: deno test -A --unstable

      - name: ls before
        shell: sh
        run: ls
        
#       - name: npm install
#         shell: sh
#         run: npm install
      
#       - name: npm lint
#         shell: sh
#         run: npm run-script lint

#       - name: npm build
#         shell: sh
#         run: npm run-script build --prod --aot
        
      - name: ls after
        shell: sh
        run: ls
      
#       - name: FTP Deploy
#       # You may pin to the exact commit or the version.
#       # uses: SamKirkland/FTP-Deploy-Action@bc95d3edc3de87143284e6707ac140ce6dcbe29c
#         uses: SamKirkland/FTP-Deploy-Action@4.0.0
#         with:
#           # ftp server
#           server: 91.219.122.5
#           # ftp username
#           username: ${{secrets.WEBIO_USER}}
#           # ftp password
#           password: ${{secrets.WEBIO_PASS}}
#           # Server port to connect to (read your web hosts docs)
# #           port: # optional
#           # protocol to deploy with - ftp, ftps, or ftps-legacy
# #           protocol: # optional
#           # Folder to upload from, must end with trailing slash /
#           local-dir: src/  # optional
#           # Path to upload to on the server. Must end with trailing slash /
#           server-dir: eoroid2144.hostingasp.pl/wwwroot/ftpgit/ # optional
#           # Path and name of the state file - this file is used to track which files have been deployed
# #           state-name: upload.log # optional
#           # Prints which modifications will be made with current config options, but doesnt actually make any changes
# #           dry-run: true # optional
#           # Deletes ALL contents of server-dir, even items in excluded with exclude argument
# #           dangerous-clean-slate: # optional
#           # An array of glob patterns, these files will not be included in the publish/delete process
# #           exclude: # optional
#           # How verbose should the information be - warn, info, debug
#           log-level: verbose # optional
#           # strict or loose
# #           security: # optional



      - name: FTP hash sync
        # You may pin to the exact commit or the version.
        # uses: socram8888/ftp-hash-sync@765613ad14a71d4750eacfca196cc5bf73077760
        uses: socram8888/ftp-hash-sync@v1
        with:
          # FTP host
          host: 
          # FTP username
          username: 
          # FTP password
          password: 
          # source path, relative to repository root
          source: src/ # optional, default is .
          # destination path
          destination: eoroid2144.hostingasp.pl/wwwroot/ftpgit4/ # optional, default is .
          # hash filename
#           hashfile: # optional, default is hashes.sha256
          # hash type
#           hashtype: # optional, default is sha256
  
  
  
      - name: FTP Deployment
      # You may pin to the exact commit or the version.
      # uses: airvzxf/ftp-deployment-action@e090fe45a9436e3f9e5d94ff1f8fcb118f25cffc
        uses: airvzxf/ftp-deployment-action@latest
        with:
          # FTP Server.
          server: 91.219.122.5
          # FTP User.
          user: ${{secrets.WEBIO_USER}}
          # FTP Password.
          password: ${{secrets.WEBIO_PASS}}
          # Local directory.
          local_dir:  src/ # optional, default is ./
          # Remote directory.
          remote_dir: eoroid2144.hostingasp.pl/wwwroot/ftpgit4/ # optional, default is ./
          # Delete all the files inside of the remote directory.
#           delete: # optional, default is false
          # Times that the lftp will be executed if an error occurred.
#           max_retries: # optional, default is 10
          # Do not create symbolic links.
#           no_symlinks: # optional, default is true
          # FTP - Allow SSL encryption.
#           ftp_ssl_allow: # optional, default is false
          # FTP - FEAT: Determining what extended features the FTP server supports.
#           ftp_use_feat: # optional, default is false
          # FTP - Delay in seconds between NOOP commands when downloading tail of a file.
#           ftp_nop_interval: # optional, default is 2
          # NET - Maximum number of operation without success. 0 unlimited. 1 no retries.
#           net_max_retries: # optional, default is 1
          # NET - Ignore hard errors. When reply 5xx errors or there is too many users.
#           net_persist_retries: # optional, default is 5
          # NET - Sets the network protocol timeout.
#           net_timeout: # optional, default is 15s
          # DNS - 0 no limit trying to lookup an address otherwise try only this number of times.
#           dns_max_retries: # optional, default is 8
          # DNS - Time for DNS queries. Set to "never" to disable.
#           dns_fatal_timeout: # optional, default is 10s



